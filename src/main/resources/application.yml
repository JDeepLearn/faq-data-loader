spring:
  application:
    name: faq-data-loader

  couchbase:
    connection-string: ${SPRING_COUCHBASE_CONNECTION_STRING:couchbase://localhost}
    username: ${SPRING_COUCHBASE_USERNAME:admin}
    password: ${SPRING_COUCHBASE_PASSWORD:password}

  data:
    couchbase:
      bucket-name: ${SPRING_DATA_COUCHBASE_BUCKET_NAME:faq_bucket}
      scope-name: ${SPRING_DATA_COUCHBASE_SCOPE_NAME:faq_scope}

logging:
  config: classpath:log4j2.xml

uploader:
  input-file: ${UPLOADER_INPUT_FILE:classpath:faq.json}
  collection: ${UPLOADER_COLLECTION:faqs}
  threads: ${UPLOADER_THREADS:4}
  auto-run: ${UPLOADER_AUTO_RUN:true}

embedding:
  service-url: ${EMBEDDING_SERVICE_URL:http://localhost:8000/embed}
  model-name: ${EMBEDDING_MODEL_NAME:e5-large-v2}
  provider: ${EMBEDDING_PROVIDER:intfloat}
  dim: ${EMBEDDING_DIM:1024}
  timeout-ms: ${EMBEDDING_TIMEOUT_MS:5000}

vector:
  index-name: ${VECTOR_INDEX_NAME:faq_vectors}

couchbase:
  fts:
    url: ${COUCHBASE_FTS_URL:http://localhost:8094}

  # Durability for KV writes (maps to DurabilityLevel):
  #   none | majority | majority_and_persist_to_active | persist_to_majority
  durability: ${COUCHBASE_DURABILITY:none}
server:
  port: 9080

